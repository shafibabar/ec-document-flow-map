# Workflow configuration. Sourced by .claude/scripts/wf-lib.sh.
#
# This is shell, not JSON, deliberately: there is no `jq` on this machine, and
# parsing JSON in pure bash is fragile. `gh --jq` handles API responses; local
# config is plain shell variables so nothing has to parse it.
#
# The ids below were read from the live API. If the board is ever rebuilt from
# scratch they must be re-read — see `wf-doctor` in CLAUDE.md.

WF_OWNER="shafibabar"
WF_REPO="ec-document-flow-map"
WF_PROJECT_NUMBER="3"
WF_PROJECT_ID="PVT_kwHOA4gFKc4BgQjv"
WF_PROJECT_URL="https://github.com/users/shafibabar/projects/3/views/1"

WF_STATUS_FIELD_ID="PVTSSF_lAHOA4gFKc4BgQjvzhadjH8"

# Board states. Names must match the option names on the board exactly.
WF_STATUS_TODO="f75ad846"
WF_STATUS_IN_PROGRESS="47fc9ee4"
WF_STATUS_IN_REVIEW="e1c1dabe"
WF_STATUS_DONE="98236657"

# Review cycles run until one finds no bugs, but stop here and escalate.
WF_REVIEW_CYCLE_CAP="5"

# Branch naming. Changing these breaks state recovery on existing branches.
WF_INTEGRATION_PREFIX="integration/"
WF_ISSUE_PREFIX="issue/"
WF_MAIN_BRANCH="main"
