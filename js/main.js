/*
 * Bootstrap. Validates MODEL, builds the graph, wires the renderer, router and
 * controls together, and starts the frame loop.
 *
 * If the model is empty or fails validation, say so on the canvas rather than
 * drawing an empty map.
 */
