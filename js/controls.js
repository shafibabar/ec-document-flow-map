/*
 * Viewer controls: scenario picker, pause / step / restart, speed, follow or
 * free the camera, pan and zoom. Pause holds indefinitely; the dwell timer at
 * each stop is visible so the viewer can see how long they have to read.
 *
 * Touch and pointer input both, sized for a phone and for a large screen.
 */

var Controls = {};
