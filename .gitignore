# Source architecture documents and the architecture image.
# These are internal and must never reach git.
knowledge/

# Workflow runtime state. Machine-local, not shared: which commits have already
# been mirrored to their issue, and an audit trail of every EC_ALLOW_MAIN use.
.claude/.mirrored
.claude/main-override.log

# Personal permission overrides; the shared workflow lives in settings.json.
.claude/settings.local.json

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
